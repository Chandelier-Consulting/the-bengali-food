"use client";
import { useSiteContent } from "./SiteContentProvider";
import Reveal from "./Reveal";
export default function ManagedMenuSections() { const { settings, menuItems, ready } = useSiteContent(); if (!ready || !settings) return <section className="px-5 py-20 text-muted">Loading menu...</section>; const groups = Array.from(new Set(menuItems.map((item) => item.category))).map((name) => [name, menuItems.filter((item) => item.category === name)] as const); return <section className="px-5 py-14 sm:px-6"><div className="mx-auto grid max-w-6xl gap-8">{groups.map(([name, items]) => <Reveal key={name}><section id={name.toLowerCase().replaceAll(" ", "-").replaceAll("&", "and")} className="overflow-hidden rounded-lg border border-border bg-surface"><div className="p-6"><p className="text-xs font-black uppercase tracking-[.18em] text-accent">{settings.businessName}</p><h2 className="mt-2 text-4xl font-black text-secondary">{name}</h2>{items.map((item) => <article key={item.id} className="grid gap-3 border-b border-border py-5 last:border-0 sm:grid-cols-[1fr_auto]"><div><h3 className="text-xl font-black text-secondary">{item.name}</h3><p className="mt-2 text-sm text-muted">{item.description}</p></div><p className="font-black text-primary">{item.price}</p></article>)}</div></section></Reveal>)}</div></section>; }
