"use client";
import { FaBriefcase, FaCalendarDay, FaUsers } from "react-icons/fa6"; import Link from "next/link"; import OrderOnlineButton from "@/components/OrderOnlineButton"; import { useSiteContent } from "@/components/SiteContentProvider";
export default function CateringPage() { const { settings } = useSiteContent(); if (!settings) return null; const occasions = [[FaBriefcase,"Office lunches","A generous Bengali spread makes team meals feel considered."],[FaUsers,"Family tables","Choose dishes for a comfort-food dinner everyone can share."],[FaCalendarDay,"Special gatherings","Build a meal around the current menu for a planned occasion."]]; const questions = ["How many people are eating?","What service option works best?","Which dishes should anchor the table?","Any dietary needs to consider?"]; return <><section className="bg-surface px-5 pb-20 pt-32"><div className="mx-auto max-w-6xl"><p className="text-xs font-black uppercase tracking-[.2em] text-accent">{settings.catering.eyebrow}</p><h1 className="mt-5 max-w-4xl text-5xl font-black text-secondary sm:text-7xl">{settings.catering.title}</h1><p className="mt-6 max-w-2xl text-lg text-muted">{settings.catering.description}</p><div className="mt-8 flex flex-wrap gap-3"><OrderOnlineButton className="inline-flex min-h-12 items-center rounded-lg bg-primary px-7 font-black text-[#142016]" label="Start an order"/><Link href="/menu" className="inline-flex min-h-12 items-center rounded-lg border border-border px-7 font-black text-secondary">Check the menu</Link></div></div></section><section className="px-5 py-24"><div className="mx-auto max-w-6xl"><h2 className="text-4xl font-black text-secondary">Made for the whole table.</h2><div className="mt-10 grid gap-5 md:grid-cols-3">{occasions.map(([Icon,title,body]) => { const Component = Icon as typeof FaBriefcase; return <article key={title as string} className="rounded-lg border border-border bg-surface p-6"><Component className="text-2xl text-accent"/><h3 className="mt-4 text-2xl font-black text-secondary">{title as string}</h3><p className="mt-2 text-muted">{body as string}</p></article>; })}</div></div></section><section className="bg-primary px-5 py-20 text-[#142016]"><div className="mx-auto grid max-w-6xl gap-8 lg:grid-cols-2"><div><p className="font-black uppercase tracking-[.18em]">Plan the order</p><h2 className="mt-4 text-4xl font-black">A few details make group ordering easy.</h2></div><div className="grid gap-3">{questions.map((question,index) => <p key={question} className="border-t border-[#142016]/25 pt-3 font-bold"><span className="mr-3 text-[#9f302a]">0{index + 1}</span>{question}</p>)}</div></div></section></>; }
