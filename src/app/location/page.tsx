"use client";
import { FaBagShopping, FaListCheck, FaUtensils } from "react-icons/fa6"; import OrderOnlineButton from "@/components/OrderOnlineButton"; import { useSiteContent } from "@/components/SiteContentProvider";
export default function OrderPage() { const { settings } = useSiteContent(); if (!settings) return null; const steps = [[FaListCheck,"Choose your dishes","Browse the current Bengali menu and find the dishes for your table."],[FaBagShopping,"Pick a service","Choose pickup or delivery directly on the live ordering menu."],[FaUtensils,"Enjoy at home","Bring the complete Bengali meal to your weeknight or gathering."]]; return <><section className="px-5 pb-16 pt-32"><div className="mx-auto max-w-6xl"><p className="text-xs font-black uppercase tracking-[.2em] text-primary">{settings.order.eyebrow}</p><h1 className="mt-5 max-w-4xl text-5xl font-black text-secondary sm:text-7xl">{settings.order.title}</h1><p className="mt-6 max-w-2xl text-lg text-muted">{settings.order.description}</p><OrderOnlineButton className="mt-8 inline-flex min-h-12 items-center rounded-lg bg-primary px-7 font-black text-[#142016]" label="Order pickup or delivery"/></div></section><section className="border-y border-border bg-surface px-5 py-20"><div className="mx-auto grid max-w-6xl gap-5 md:grid-cols-3">{steps.map(([Icon,title,body]) => { const Component = Icon as typeof FaListCheck; return <article key={title as string} className="border-t border-border pt-5"><Component className="text-2xl text-accent"/><h2 className="mt-4 text-2xl font-black text-secondary">{title as string}</h2><p className="mt-2 text-muted">{body as string}</p></article>; })}</div></section></>; }
