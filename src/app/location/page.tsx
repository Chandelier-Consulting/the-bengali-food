"use client";
import OrderOnlineButton from "@/components/OrderOnlineButton"; import { useSiteContent } from "@/components/SiteContentProvider";
export default function OrderPage() { const { settings } = useSiteContent(); if (!settings) return null; return <section className="px-5 pb-24 pt-32"><p className="text-xs font-black uppercase tracking-[.2em] text-primary">{settings.order.eyebrow}</p><h1 className="mt-5 text-5xl font-black text-secondary sm:text-7xl">{settings.order.title}</h1><p className="mt-6 max-w-2xl text-lg text-muted">{settings.order.description}</p><OrderOnlineButton className="mt-8 inline-flex min-h-12 items-center rounded-lg bg-primary px-7 font-black text-[#142016]" label="Order pickup or delivery"/></section>; }
