"use client";
import ManagedMenuSections from "@/components/ManagedMenuSections"; import OrderOnlineButton from "@/components/OrderOnlineButton"; import { useSiteContent } from "@/components/SiteContentProvider";
export default function MenuPage() { const { settings, menuItems, ready } = useSiteContent(); if (!ready || !settings) return null; const categories = [...new Set(menuItems.map((item) => item.category))]; return <><section className="px-5 pb-16 pt-32"><p className="text-xs font-black uppercase tracking-[.2em] text-accent">{settings.businessName}</p><h1 className="mt-5 text-5xl font-black text-secondary">Bengali dishes, ready when you are.</h1><p className="mt-5 max-w-2xl text-muted">Explore the menu, then open the live ordering menu for current availability and checkout.</p><OrderOnlineButton className="mt-7 inline-flex min-h-12 items-center rounded-lg bg-primary px-7 font-black text-[#142016]" label="Open live ordering menu"/></section><nav className="flex gap-2 overflow-x-auto border-y border-border px-5 py-4">{categories.map((name) => <a key={name} href={`#${name.toLowerCase().replaceAll(" ", "-").replaceAll("&", "and")}`} className="shrink-0 rounded-lg border border-border px-5 py-3 text-sm font-black">{name}</a>)}</nav><ManagedMenuSections/></>; }
